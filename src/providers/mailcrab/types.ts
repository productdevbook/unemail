import type { EmailOptions } from 'unemail/types'

/**
 * MailCrab-specific email options
 */
export interface MailCrabEmailOptions extends EmailOptions {
  // No specific options beyond the common EmailOptions
}
